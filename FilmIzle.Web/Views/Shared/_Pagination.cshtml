@{ 
    int page = (int)ViewBag.filmPage;
    int filmPageCount = (int)ViewBag.filmPageCount;
    string action = (string)ViewBag.Action;
}


@if (filmPageCount != 0)
{
    <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center mt-3">
            @if (filmPageCount == 1)
            {
                <li class="page-item disabled">
                    <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                        <span aria-hidden="true">&laquo;</span>
                        <span class="sr-only">Previous</span>
                    </a>
                </li>
                <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                <li class="page-item disabled">
                    <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                        <span class="sr-only">Next</span>
                    </a>
                </li>
            }
            else if (filmPageCount == 2)
            {
                if (page == 1)
                {
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)">@(page + 1)</a></li>
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
                else
                {
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)">@(page - 1)</a></li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
            }
            else if (filmPageCount == 3)
            {
                if (page == 1)
                {
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)">@(page + 1)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 2)">@(page + 2)</a></li>

                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
                else if (page == 2)
                {
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)">@(page - 1)</a></li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)">@(page + 1)</a></li>
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
                else
                {
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 2)">@(page - 2)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)">@(page - 1)</a></li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
            }
            else if (filmPageCount == 4)
            {
                if (page == 1)
                {
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)">@(page + 1)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 2)">@(page + 2)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 3)">@(page + 3)</a></li>

                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
                else if (page == 2)
                {
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)">@(page - 1)</a></li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)">@(page + 1)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 2)">@(page + 2)</a></li>
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
                else if (page == 3)
                {
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 2)">@(page - 2)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)">@(page - 1)</a></li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)">@(page + 1)</a></li>
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
                else
                {
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 3)">@(page - 3)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 2)">@(page - 2)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)">@(page - 1)</a></li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
            }
            else
            {
                if (page == 1)
                {
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)">@(page + 1)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 2)">@(page + 2)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 3)">@(page + 3)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 4)">@(page + 4)</a></li>

                    <li class="page-item">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
                else if (page == filmPageCount)
                {
                    <li class="page-item">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 4)">@(page - 4)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 3)">@(page - 3)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 2)">@(page - 2)</a></li>
                    <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)">@(page - 1)</a></li>
                    <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                    <li class="page-item disabled">
                        <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                }
                else
                {
                    if ((filmPageCount - 1) == page)
                    {
                        <li class="page-item">
                            <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                                <span aria-hidden="true">&laquo;</span>
                                <span class="sr-only">Previous</span>
                            </a>
                        </li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 3)">@(page - 3)</a></li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 2)">@(page - 2)</a></li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)">@(page - 1)</a></li>
                        <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)">@(page + 1)</a></li>
                        <li class="page-item">
                            <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                                <span aria-hidden="true">&raquo;</span>
                                <span class="sr-only">Next</span>
                            </a>
                        </li>
                    }
                    else if (page == 2)
                    {
                        <li class="page-item">
                            <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                                <span aria-hidden="true">&laquo;</span>
                                <span class="sr-only">Previous</span>
                            </a>
                        </li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)">@(page - 1)</a></li>
                        <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)">@(page + 1)</a></li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 2)">@(page + 2)</a></li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 3)">@(page + 3)</a></li>
                        <li class="page-item">
                            <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                                <span aria-hidden="true">&raquo;</span>
                                <span class="sr-only">Next</span>
                            </a>
                        </li>
                    }
                    else
                    {
                        <li class="page-item">
                            <a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)" aria-label="Previous">
                                <span aria-hidden="true">&laquo;</span>
                                <span class="sr-only">Previous</span>
                            </a>
                        </li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 2)">@(page - 2)</a></li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page - 1)">@(page - 1)</a></li>
                        <li class="page-item active"><a class="page-link" asp-action="@(action)" asp-route-page="@(page)">@(page)</a></li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)">@(page + 1)</a></li>
                        <li class="page-item"><a class="page-link" asp-action="@(action)" asp-route-page="@(page + 2)">@(page + 2)</a></li>
                        <li class="page-item">
                            <a class="page-link" asp-action="@(action)" asp-route-page="@(page + 1)" aria-label="Next">
                                <span aria-hidden="true">&raquo;</span>
                                <span class="sr-only">Next</span>
                            </a>
                        </li>
                    }
                }
            }
        </ul>
    </nav>
}
else
{
    <h1 class="text-white text-center mt-5">Herhangi Bir Film Bulunamadı</h1>
}